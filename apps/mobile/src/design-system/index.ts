/**
 * CarBuddy's Material 3 Expressive design system.
 *
 * Screens import from here and nowhere deeper. Everything visual — colour,
 * type, shape, spacing, elevation, motion, state — comes from a token, so the
 * app can be re-themed (dark mode, dynamic colour, higher contrast) without
 * touching a single screen.
 */
export * from './tokens';
export * from './theme';
export * from './components';
